service: sat-graphql-api
frameworkVersion: '3'

plugins:
  - serverless-esbuild
  - serverless-offline

custom:
  esbuild:
    config: node_modules/@sat/serverless-esbuild-config/config.js

provider:
  name: aws
  runtime: nodejs18.x

functions:
  graphql-endpoint:
    handler: src/index.default
    environment:
      SENTRY_DSN: ${env:SENTRY_DSN}
      SENTRY_ENVIRONMENT: ${sls:stage}
    events:
      - httpApi:
          path: /
          method: post
