service: graphql-api
frameworkVersion: '3'

plugins:
  - serverless-esbuild
  - serverless-offline

custom:
  esbuild:
    config: node_modules/@sat/serverless-esbuild-config/config.js

provider:
  name: aws
  runtime: nodejs18.x

functions:
  graphql-endpoint:
    handler: src/index.default
    environment:
      FUNCTION_NAME: graphql-endpoint
      SENTRY_DSN: https://8a9c081ca06447d18a5331ac7dff82f7@o4504794176684032.ingest.sentry.io/4504843192762368
      SENTRY_ENVIRONMENT: ${sls:stage}
    events:
      - httpApi:
          path: /
          method: post
